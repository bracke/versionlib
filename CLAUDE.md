# CLAUDE.md

The guidance for this crate lives in `AGENTS.md` — the vendor-neutral file that
every AI coding tool reads, not just this one. It is imported below so Claude
Code loads exactly the same text, which leaves one copy to keep accurate instead
of two that drift apart.

@AGENTS.md
