- Fix: a `--rebase-merges` conflict rolls the rebase back instead of pausing. Its paused todo is still written as plain picks while git expresses the topology as `label`/`reset`/`merge`, so a rebase paused by this tool and resumed with real git replayed the merges linearly and flattened the history without saying so. Rolling back is worse to use than pausing, but it leaves nothing git can misread; pausing returns once the todo port lands.
- Add: `rebase --skip` and `rebase --quit`. Skip drops the commit the rebase stopped on and replays the rest; quit forgets the rebase without undoing it, leaving HEAD where the replay reached and the branch at its original tip. Both match git, including the resulting commit ids -- and `status` was already telling users about `--skip`.
- Fix: rebase writes git'''s reflog messages -- `rebase (start): checkout <onto>` naming the ref the user gave rather than the id it resolved to, `rebase (pick): <subject>` per replayed commit, and git'''s two distinct finish lines (HEAD returns to the ref, the branch records what it was rebased onto). One entry for the whole rebase was written before.
- Change: a rebase now keeps its state in git'''s own `.git/rebase-merge/` directory and detaches HEAD at `onto` for the duration, as git does, instead of a private `VERSION_REBASE` file with HEAD left on the branch. A rebase either tool pauses is now the same object to both: `git status` reports it, and `git rebase --continue`/`--abort` finish it -- and the reverse -- producing identical commit ids. This also removes the spuriously-staged files that a paused rebase used to show, since the index no longer describes a different commit than HEAD.
- Fix: `rebase --continue` commits what was staged, as git does, and refuses while the index still holds unmerged stages. It rebuilt the index from the working tree instead, which both accepted a resolution that was never marked resolved and swept up every unrelated edit lying around.
- Fix: gc keeps a paused rebase'''s in-flight commits. Reachability scanned the old state file; partway through a rebase those commits are on no branch, so the new state directory is what holds them alive.
- Fix: a rebase that stops on a conflict now records the conflicted paths at stages 1/2/3. The merged index was computed but never written, so the index still held the replay parent'''s tree at stage 0 -- the marked-up file read as an ordinary modification, no git tool could see a conflict was in progress, and `commit` would have taken the conflict markers silently.
- Fix: a submodule's `.git` file now points at the administrative directory relatively, as git's does. An absolute pointer survives only until the superproject is moved or cloned elsewhere, at which point every submodule in it breaks.
- Fix: `.gitmodules` keeps a section's `branch` key. It was parsed as an unknown key and dropped, so rewriting a file git had written silently discarded it.
- Add: `Version.Submodules.Add` takes Branch, Name and Force. Name is the submodule's logical name and keys both the config and the administrative directory, so it parts company with the path once `--name` is given.
- Fix: `Version.Submodules.Add` adopts a path that is already a repository instead of refusing it, as git does; only a non-repository directory in the way is an error.
- Add: `Version.Worktrees.Move` and `Repair`. Move relocates a linked worktree, rewriting only the administrative entry's `gitdir` pointer -- the entry keeps its name, since the name is not what records where the worktree lives. Repair re-points a worktree and its entry at each other after the directory was moved by hand, reading the worktree's own `.git` file, whose link to the entry survives such a move and is what makes the repair possible.
- Fix: a paused `bisect` now leaves the state files git expects, so a bisection started here can be continued with git and vice versa.
- Fix: `bisect` writes `BISECT_EXPECTED_REV` and `BISECT_ANCESTORS_OK`. `Clear` already removed both, so the intent was there, but nothing ever wrote them: a bisection paused by version left a state git could not reason about, since git compares the expected rev against HEAD to catch a verdict given after checking out something else. `Record_Expected` records the commit handed out at each step, and it now advances exactly as git's does.
- Fix: `fetch <remote> <branch>` updates only that branch's remote-tracking ref. It previously created one for every branch the remote advertised, so a repository picked up tracking refs nobody asked for.
- Add: `Version.Fetch.Fetch_Branch` restricts a fetch to one branch's remote-tracking ref, as `fetch <remote> <branch>` does. A plain fetch collects every advertised branch, and the advertised-ref path selected them by comparing object ids, so naming one branch still created tracking refs for the others -- and any branch sharing that commit came along regardless.
- Fix: `repack` removes the packs its new pack supersedes, so repacking shrinks the object store instead of growing it. The new pack was published alongside the old ones and every object ended up stored twice -- `count-objects` on an already-packed repository went from 9 in-pack in 1 pack to 18 in 2 -- so `gc` and `repack` could never reclaim anything. A pack is removed only once the new one is proved to contain every object it held, which is stricter than `git repack -a -d`: that drops unreachable objects outright, whereas a pack still holding some is kept here. Verified against git (1 pack, 9 in-pack, identical), with `git fsck` clean and no reachable object lost across repeated repacks.
- Fix: a relative submodule URL resolves against the base treated as a directory, as git does. `https://example.com/group/super.git` with `../libfoo.git` is `https://example.com/group/libfoo.git` -- verified against git, which reports exactly that URL when the clone fails. The resolver stripped the repository name off the base first and then applied the `../`, removing one component too many, so every relative submodule URL pointed a directory too high; the `.git` suffix makes no difference to git, which rules out the stripping interpretation. Seventeen unit tests encoded the old arithmetic and have been corrected, including three whose fixture layout expressed it as directory structure. The traversal-escape rejections are kept -- git merely normalises and lets the clone fail -- with each case given one more `../` so it still describes the escape it was written to describe.
- Add: `Version.Submodules.Add`, git's `submodule add`: clone the URL into the path, record it in `.gitmodules`, stage the gitlink and that file together, and register the resolved URL and `active` flag in the local config. Nothing in the library could create a submodule before -- only read, update and remove ones that already existed. The `.gitmodules` entry keeps the URL exactly as given, so a relative one stays relative and the superproject stays portable, while the config holds the resolved form used to fetch.
- Fix: a relative submodule URL resolves against the superproject's own location when it has no configured remote, as git does, instead of refusing. That refusal made relative URLs -- the normal spelling in a real project -- unusable in any repository that has never been pushed, which includes every one created locally.
- Add: `Version.Worktrees.Lock`/`Unlock`/`Is_Locked`, git's mark for a worktree that must not be removed or pruned, and `Worktree_Info.Locked` so a listing can report it. `Remove` now refuses a locked worktree with git's reason -- it previously said the working tree was unclean, naming the wrong cause and pointing at the wrong remedy.
- Add: `Worktree_Info.Head` carries the commit a worktree has checked out. git's `worktree list` reports it for every entry, attached or detached, so it could not be read off `Branch` alone.
- Add: `Version.Worktrees.Prunable`/`Prune`, and `Remove` reclaims a worktree whose directory is already gone. Deleting a linked worktree's directory left its admin entry behind holding the branch checked out, and no command could clear it -- `remove` refused because the `.git` file was missing and there was no `prune` at all, so the only way back was deleting `.git/worktrees/<name>` by hand.
- Fix: `Reject_Nested_Worktree` no longer refuses a worktree inside the repository or inside another linked worktree. git accepts both and both work; the check turned `worktree add wt <branch>` -- the spelling in git's own documentation -- into an error.
- Add: `Version.Notes` gains `List`, `Has_Note`, `Remove`, `Append`, `Copy` and `Prune`, and every operation takes the notes ref (git's `--ref=<name>`, where a bare name means `refs/notes/<name>`) instead of assuming `refs/notes/commits`. Only `Add` and `Show` existed, so a caller could write a note and read it back and do nothing else with it.
- Fix: `Version.Bisect.Compute` rejects inconsistent marks instead of computing on them. A good rev that is not an ancestor of the bad one -- the same commit marked both ways being the plain case -- describes no bisectable range, and carrying on produced a confident "is the first bad commit" for a question that was never well posed. git refuses with "Some good revs are not ancestors of the bad rev."
- Fix: history walks honour `refs/replace/*`. `Version.Objects.Read_Object` resolved a replacement, but `Version.Object_Cache.Read_Object` -- which every walk uses for speed -- read the original object, so a `replace --graft` was honoured by `cat-file` and invisible to `rev-list`, `cherry`, `log` and `filter-branch`. Since a graft changes reachability, those commands answered a different question than git. The cache now resolves the replacement and stays keyed on the id it was asked for, so the mapping costs one lookup.
- Fix: `Version.Cherry.Status` lists commits with `Version.History.Rev_List` instead of the rebase replay walk. That walk follows first parents and refuses a root or a merge, so `cherry` failed outright with "root rebases not supported" on any history reaching a root before the merge base -- which a graft produces and unrelated histories always do.
- Fix: `Version.Remotes.Rename_Remote` rewrites the refspec and carries the remote-tracking refs across. A refspec names the remote inside its own value, so copying it verbatim left the renamed remote fetching into `refs/remotes/<old>/*` -- a namespace nothing owns any more -- while every existing tracking ref stayed stranded under the old name. Deleting the last ref under `refs/remotes/<name>/` now also removes the emptied directory, so `remove` and `rename` no longer leave the remote visible in a listing of that tree. The unit test that required the old refspec has been corrected: it encoded the bug.
- Fix: the `Packed_Refs` unit test asserted the old `# pack-refs with: sorted` header. It had been passing against a stale test binary; rebuilding the test crate against the library surfaced it.
- Fix: `Version.Submodules.Deinit` refuses a submodule whose checked-out HEAD has moved off the recorded gitlink, not only one with a dirty work tree. The superproject records just the gitlink, so clearing the directory in that state discards a checkout it cannot reconstruct; git refuses both alike and wants `--force`.
- Fix: `Version.Bundle` checks that a bundle path is a regular file before reading it. Reading a directory never ends, so `bundle verify <dir>` exhausted the heap and died with STORAGE_ERROR where git simply says the file is not a bundle.
- Add: `Version.Grep.Line_Matcher` with `Compile`/`Matches`, a pattern compiled once and applied to many lines. `log --author=`/`--grep=` filter every commit in a walk, and both are regular expressions in git, so neither per-line recompilation nor a substring approximation would do.
- Add: `Version.Show.Show_Commit` takes `No_Patch`, `Oneline` and `Format`, so a caller can shape the header (git's `show -s`, `--oneline`, `--format=<fmt>`) instead of always emitting the full one.
- Add: `Version.Mailbox.Parse` takes `Keep_Subject`, git's `mailinfo -k`, leaving the Subject header exactly as it arrived instead of stripping bracket prefixes and a leading "Re:".
- Fix: `Version.Ref_Format` implements `%(upstream:track)` and `:trackshort` via `Version.Tracking.Count_Ahead_Behind` instead of falling through to the upstream refname, and shortens `refs/remotes/<remote>/HEAD` to `<remote>` as git does.
- Fix: `Version.Show.Show_Commit` omits the patch for a commit with more than one parent, as git does; there is no single "the" diff for a merge and the first parent's is one side's changes wearing the merge's name. A requested `--stat` is still produced.
- Fix: `Version.Shortlog.Summarize` walks history with `Version.History.Rev_List` instead of its own depth-first crawl, so the per-group reversal to oldest-first actually yields chronological order.
- Fix: `Version.Describe` breaks an equal-distance tie by tagger date, newest first, matching git; previously two annotated tags on one commit resolved by tag name.
- Fix: `Version.Packed_Refs.Write_All` emits git's full header (`peeled fully-peeled sorted `) and a `^<target>` peel line for each annotated tag, so a reader may trust the absence of a peel line instead of re-inflating the tag. `Pack_Refs` gained `Include_All` for git's `--all`, which packs everything under refs/ rather than only heads and tags -- remote-tracking refs, notes and stashes were previously left loose by a flag documented as packing every ref.
- Fix: `Version.Ref_Format` implements `%(refname:lstrip=N)` and `:rstrip=N`, including git's negative counts (keep N components from the other end) and empty result when N exceeds the component count. Previously every modifier but `short` silently returned the whole refname, and an unrecognised one still does not raise -- it now does, since a plausible wrong answer is harder to notice than a refusal.
- Fix: `Version.Apply.Apply_Patch` rejects patches it cannot apply instead of quietly applying nothing. A header-only binary diff, input with no patch in it, a hunk whose declared line counts disagree with its body, and a `-p<n>` deeper than the path all returned normally having changed no file. `Apply_Options.Allow_Empty` covers git's `--allow-empty`.
- Fix: `Version.Revert` writes the merged index on the conflict path, so a paused revert carries index stages 1/2/3 with `REVERT_HEAD` and `MERGE_MSG`, as `Version.Cherry_Pick` already did. Without it the index held a stage-0 entry and the conflict was invisible to `status`.
- Fix: `Version.Move.Move_Path` refuses a conflicted source and no longer invents the destination's parent. It matched the source by path alone, so for a conflicted path it picked stage 1 -- the merge base -- and restaged the move as a stage-0 entry holding base content, dropping stages 2 and 3; the index then claimed a resolution nobody made while the file still held conflict markers. It also created the destination's parent directories, where git reports the rename(2) failure. `Create_Parents` re-enables creation for the one caller that needs it, a directory rename replayed file by file, which git performs with a single rename(2).
- Fix: `Version.Reset.Reset_To_Commit` clears the merge, cherry-pick and revert state for every mode but `--soft`, matching git's `remove_branch_state()`. Without it a paused replay survived the reset that discarded it.
- Add: `Version.Files.Write_Symlink` creates a symbolic link, the primitive missing for materialising a mode 120000 entry outside the worktree root (`checkout-index --prefix`). `Version.Restore` has an equivalent private one that resolves against the repository root and so could not serve it.
- Fix: `Version.Ref_Names.Is_Valid_Ref_Name` no longer restricts refs to an allowlist of hierarchies. git accepts any name satisfying check-ref-format with more than one component, and the allowlist meant a single `refs/bisect/*`, `refs/pull/*`, `refs/stash` or third-party ref made every caller that enumerates refs fail outright instead of listing what it could. Validation now defers to `Is_Valid_Check_Ref_Format`, which already implemented git's rules for the `check-ref-format` command itself.
- Add: `Version.Blame.Blame_Working_File` blames a file as it stands in the working tree, which is what `git blame <file>` reports. Lines that correspond to no line at the tip come back with the all-zero id, git's "Not Committed Yet". The history walk is now shared with `Blame_File`, which is the special case where the reported text is the tip's own. The package header also no longer claims a simpler content-based attribution than the code performs -- it has followed git's line correspondence (`Version.Merge.Align_Lines`) for some time.
- Fix: `Version.Checkout` refuses a switch only for paths the target would write over, instead of on any unclean working tree, and reports which ones in git's words. The rule is exposed as `Require_Switch_Safe` and `Version.Branch.Switch_Branch` now shares it rather than keeping a second, stricter copy. It also returns the paths whose local edit rides across the switch; `Version.Restore.Restore_Working_Tree_For_Commit` grew a `Keep` parameter to leave exactly those files alone, without which materializing the target tree would overwrite the edits the new rule had just allowed to survive.
- Fix: `Version.Archive.Create` writes straight to an output that is not a regular file. It always built beside the target and renamed into place, which cannot address `/dev/null`: it needed to create `/dev/null.version-archive-tmp`, and the rename would have replaced the device node with a regular file. Regular targets keep the build-and-rename path, so a failure still leaves no truncated archive.
- Add: `Version.Remove.Modification_Of` classifies a path against a computed status as git's `check_local_mod` does -- staged, locally modified, or staged content differing from both the file and HEAD -- so a caller can refuse to delete unrecoverable content and can classify every path before deleting any. `Remove_Path` gained a `Cached_Only` form for git's `--cached`.
- Add: `Version.Pathspec.Resolve_Against_Prefix` is public. Folding a path operand into the directory a command ran in and collapsing its `.`/`..` segments was already implemented for pathspecs but private to the body; operands that are plain paths rather than pathspecs -- `blame`'s file -- need the same resolution without the magic-prefix parsing, which git reads literally in that position.
- Fix: `Version.Maintenance.Repack` writes `objects/info/packs` (the `P <pack>` list a dumb-HTTP client reads to discover packs, which no repack had ever refreshed) and the commit-graph, as git's gc does. The graph verifies clean under real `git commit-graph verify`. Note the pack itself is still named `version-repack.pack` rather than git's `pack-<checksum>.pack`; both are read fine by git, which globs the directory.
- Fix: `Version.Multi_Pack_Index.Verify` checks the file's structure, not just its trailing checksum. A correct checksum only says the bytes are the ones that were written, so a multi-pack-index whose object fanout was out of order passed silently whenever the hash had been recomputed -- the shape a broken writer produces. The fanout is now required to be monotonic, reported as git reports it (`oid fanout out of order`).
- Fix: `Version.Fmt_Merge_Msg.Format` groups refs by source the way git does. Every kind fetched from one URL is now listed together with a single trailing `of <url>` (`Merge branch 'a', tag 't1' of U`), where each kind previously carried its own `of <url>` and the kinds were joined with `; `. A description consisting of just a URL is a HEAD fetch: it is reported as the URL alone, or as `HEAD` beside named refs from the same URL, instead of being dropped so that the message came out empty.
- Fix: `Version.Mailbox.Parse` decodes RFC 2047 encoded-words in the Author and Subject headers (both `Q` and `B` forms, transcoding an ISO-8859-1 word to UTF-8 as git does), strips every leading bracket group rather than only the first, and drops a leading `Re:` in any case -- so `[PATCH 1/1] [RFC] ...` and `re: RE: [PATCH] ...` clean up the way git cleans them.
- Fix: `Version.Maintenance.Repack` removes the loose copies of the objects it has just packed, as git's `repack -d` does. Without it a repository never shrank: after `gc`, `count-objects -v` still reported every object as loose *and* packed, with `prune-packable` equal to the whole object count. Objects are deleted only after the new pack has been verified to contain them and published. Unreachable objects are untouched, matching git, which keeps them for two weeks.
- Fix: `diff-files` honours its pathspec operands. They were parsed and then ignored, so `diff-files -- a.txt` reported every changed file in the working tree.
- Fix: `ls-tree` matches path operands literally, as git does, instead of expanding them as globs -- `ls-tree HEAD '*.txt'` listed files git omits entirely. A nested operand (`d/sub`, `d/x.txt`) now resolves by walking down the tree, where before it produced nothing without `-r`.
- Add: `Version.Diff.Raw_Diff_Files` takes an optional pathspec vector and restricts its output to matching paths, as git's `diff-files -- <path>` does.
- Fix: `interpret-trailers` inserts a trailer before a `---` patch divider instead of after the diff, unfolds continuation lines in normal output (not only under `--only-trailers`, and now stripping a leading tab), and emits the blank separator line git writes even when adding no trailer.
- Fix: `Version.Trailers.Interpret` now stops the commit message at a `---` line, so a trailer is inserted into the trailer block before the patch rather than appended after the diff. `Unfold` also applies to whole-message output (it previously only worked under `Only_Trailers`) and strips a leading tab as well as spaces, and the blank separator line git always emits before the trailer position is now written even when no trailer is added. Verified against git across ten message shapes and nine option combinations (90 comparisons).
- Fix: `clone`, `checkout` and `restore` write an executable file with git's mode (`0777 & ~umask`, so 755 under umask 022 and 775 under 002) instead of setting only the owner's exec bit (744/764).
- Fix: a checked-out executable now gets git's permissions. `Version.Files.Set_Executable` applies `0777 & ~umask` (or `0666 & ~umask` when clearing), instead of a fixed 0755/0644, and the checkout and merge paths use it rather than `GNAT.OS_Lib.Set_Executable`, which sets only the owner bit. An executable restored by `clone`/`checkout`/`restore` was 744 under umask 022 and 764 under 002, where git writes 755 and 775.
- Fix: `merge --no-commit` fast-forwards when the target is a descendant, as git does, instead of leaving HEAD behind with `MERGE_HEAD` written and the change staged. `--no-ff --no-commit` still pauses, and `--squash` is unchanged.
- Fix: `merge-recursive` and `merge-tree` report a path that survives on only one side as `CONFLICT (modify/delete): <path> deleted in <side> and modified in <other>.  Version <other> of <path> left in tree.`, matching git, instead of calling it a content conflict. The index stages were already correct.
- Fix: `merge --no-commit` no longer blocks a fast-forward. git cannot stop a fast-forward with `--no-commit` -- there is no merge commit to withhold, so the branch simply moves -- but version left HEAD in place, wrote `MERGE_HEAD` and staged the change, leaving the repository in a merge state git would not have created. Only `--no-ff` turns the update into a real merge, which still honours `--no-commit`; `--squash` is unaffected.
- Fix: `cherry-pick` conflict state is readable by git. A conflicted pick now writes the conflicted index (stages 1/2/3), `CHERRY_PICK_HEAD` and `MERGE_MSG`, so `git status` shows `UU` rather than a plain modification; `--continue`/`--abort` clear them again. A pick that turns out to be empty (already applied) now stops with exit 1 and a paused state instead of recording an empty commit and exiting 0.
- Fix: a conflicted `cherry-pick` now leaves the state git expects -- the conflicted index with stages 1/2/3, `CHERRY_PICK_HEAD`, and `MERGE_MSG`. The merged index was computed and then discarded, so the conflict never reached the index and `git status` reported a plain modification instead of `UU`; only the private `VERSION_*` files recorded the pause. `Version.Merge_State.Clear_State` also removes `CHERRY_PICK_HEAD`/`REVERT_HEAD`, so `--continue`/`--abort` leave the repository clean as git does.
- Fix: `Version.Cherry_Pick` refuses to record an empty commit. A pick whose tree matches its parent's (already applied, or emptied by conflict resolution) now pauses with `CHERRY_PICK_HEAD`/`MERGE_MSG` set, as git does, instead of silently adding an empty commit to the history.
- Fix: `ls-files` handles a filename containing a control character. `ls-files -m` failed outright (`error: path contains control character`, exit 1) because reading the tree rejected the name; it now lists like git. Paths needing it are C-quoted on output (`"name\twith\ttab.txt"`), for `ls-files` and `ls-files -s`, matching git for every byte value.
- Add: `Version.Path_Quoting.Quote_C_Style` -- git's `quote_c_style`: a path holding control characters, a quote, a backslash, DEL or a high-bit byte is printed in double quotes with C escapes (`\t`, `\n`, `\NNN`, ...), matching git's `core.quotePath` default. Verified against git for every one of the 255 byte values legal in a filename.
- Fix: reading a tree entry whose name holds a control character (a tab in a filename is a legal POSIX name git happily stores) no longer raises `path contains control character`. That write-side safety check was being applied to the read path, making any tree containing such a file unreadable -- `ls-files -m` failed with exit 1 where git lists normally. `Require_Safe_Relative_Path`/`Normalize_Relative_Path` gained `Allow_Control`, set only on the tree-entry read path; NUL is still refused everywhere and write paths keep the stricter default.
- Fix: `name-rev` walks all parents instead of first-parent history only. A commit reachable solely through a merge's second parent is now named `<tip>^2` as git names it, rather than falling back to a branch name -- or, under `--tags`, to `undefined`.
- Add: `Version.Name_Rev` -- a port of git's `name-rev` (builtin/name-rev.c). It walks every parent, not just the first, so a commit reachable only through a merge's second parent is named `<tip>^2` as git names it; includes git's merge-traversal distance weighting, its `is_better_name` preference, and the `cmp_by_tag_and_age` tip ordering that decides ties (a lightweight tag, carrying its commit's older date, beats an annotated tag on the same commit). Matched git on 1790 of 1792 fuzzed lookups; the two residuals are same-second timestamp ties that git itself resolves by qsort order.
- Fix: `clone` no longer copies the user's global `~/.gitconfig` into the new repository's `.git/config` (it wrote the merged system+global+local config back to the local file, twice -- leaking user identity and credential helper commands into every clone). A cloned `.git/config` is now byte-identical to git's.
- Fix: `Version.Config.Replace_Section` and `Remove_Section` now read only the repository's own config file. They read the MERGED config (system + global + local) and wrote it back to the local file, so any caller -- `clone` setting `remote.origin.*`/`branch.*`, `remote add`, tracking setup, sparse config -- copied the user's entire `~/.gitconfig` (identity, credential helper commands, everything) into the repository's `.git/config`, and repeated callers duplicated it. Beyond the leak this also froze global settings into the repo, where they then overrode later global changes.
- Fix: `Version.Config`'s writer no longer emits a blank line before each new `[section]`, matching git's `config` output byte for byte.
- Fix: commit, tag, reflog and `var` timestamps are now real Unix time. They were built from a local-zone 1970 epoch, so every object version wrote carried a timestamp about an hour in the future (the zone's 1970 UTC offset) while still reporting the correct `+hhmm` zone -- so `log`, `git log` and any date-ordered walk saw commits dated ahead of the wall clock.
- Fix: new `Version.Timestamps` -- every timestamp version writes was in the future. `Ada.Calendar.Time_Of (1970, 1, 1)` builds the epoch in the *local* zone, so `Clock - Epoch` was short by the zone's UTC offset as it stood in January 1970 (an hour in central Europe, and not the current offset, since DST differs); commits, tags, reflog entries, `var`, stash and index mtimes were all stamped ~3600s ahead of the wall clock. The epoch is now built at UTC, and the Duration-to-integer conversion floors instead of rounding (Ada rounds, which could still stamp a commit up to a second early/late). All nine sites across both crates now share these helpers.
- Add: `Version.Diff` emits git's `GIT binary patch` block (new `Diff_Options.Binary_Patch`, git's `--binary`, which `format-patch` implies): the full index line, then base85-encoded deflated forward and reverse literal blocks, instead of the `Binary files ... differ` line. The deflated payload is deliberately not byte-identical to git's -- a zlib stream is not canonical -- but decodes to the same bytes; verified by round-tripping 240 fuzzed add/modify/delete binary changes through `git am` and comparing the resulting trees, plus `git apply` and our own `apply`.
- Fix: `Version.Diff`'s `--stat` now scales the `+`/`-` bar graph into git's graph column instead of printing one mark per changed line, and elides an over-long name head-first as `...`, porting git's `show_stats` width budget (`$COLUMNS`, else 80). Long-name and many-line diffs previously produced far wider stat lines than git.
- Add: `Version.Rename_Detect` -- git-compatible rename detection, a port of `diffcore-delta.c` (the spanhash content-similarity counter) and the exact/inexact pairing passes of `diffcore-rename.c`, so the `similarity index NN%` we report is the number git reports. Verified against `git diff -M01%` over 1200 fuzzed file pairs.
- Add: `Version.Diff` now detects renames by default, as git does. `Diff_Options` gains `Detect_Renames` (tri-state: honour `diff.renames`, or force on/off as `-M`/`--no-renames`), `Rename_Score` and `Rename_Limit`. Patch output gains the `similarity index`/`rename from`/`rename to` block (and omits the index and hunks entirely for a pure rename), `--stat` and `--summary` gain git's `a => b` and brace-compressed `d/{a => b}` names plus the ` rename ... (NN%)` line, and `--name-status` gains `R<nnn>` with both paths.
- Fix: `Version.Diff` stat/name-status now report a mode-only change (identical blob, differing mode) as a modification, matching git; previously such a change was silently absent from `--stat`, `--name-status` and `--name-only` (the patch body was already correct).
- Fix: `Version.Diff`'s `--stat` footer now prints ` 0 insertions(+), 0 deletions(-)` when a change has neither (git's `print_stat_summary` shows each clause when the other is zero), and omits the separating space before an empty +/- bar.
- Add: `Version.History.Rev_List` -- git's `rev-list A... ^B...`: a committer-date-ordered walk of both the included and excluded frontiers, with `Max_Count`, `No_Merges`, `First_Parent` and `Oldest_First`. Unlike the existing first-parent walks it traverses every parent of a merge, so it sees side-branch history.
- Add: `Version.Objects.Commit_Committer_Time` -- the committer timestamp of a commit, which is the order git's rev-list uses.
- Fix: `Version.Format_Patch.Patch_For_Commit` now emits git's diffstat + summary block (the same content as `git diff --stat --summary`) between the `---` line and the patch body, followed by a blank line, matching `git format-patch`; previously it went straight from `---` to the diff. A commit message body now also runs straight into the `---` line, as git writes it, instead of being followed by a spurious blank line.
- Add: `Version.Diff.Diff_Tree_Vs_Working` and `Diff_Tree_Vs_Index` -- the unified diff of an arbitrary tree against the working tree (`git diff <commit>`, `git diff-index -p <tree>`) or against the index (`git diff-index -p --cached <tree>`), reusing the existing `Diff_Sides` engine.
- Fix: `Version.Revisions` now resolves the `<rev>^0` suffix by peeling `<rev>` to its commit (equivalent to `^{commit}`), matching git, instead of raising `invalid parent index`. This fixes `rev-parse`/`name-rev`/any command given e.g. `HEAD^0` across the CLI.
- Fix: `Version.Diff.Raw_Diff_Files` and the working-tree branch of `Raw_Diff_Index` now compute the new side's mode from `Working_Disk_Mode` and report a mode-only change; `Raw_Diff_Index` prints a zero working-side id unless the file matches the index in both content and mode.
- Fix: `Version.Diff` emits `old mode`/`new mode` lines for a file-mode change (pure or with content), and the working-tree side now adopts the file's on-disk mode (new `Working_Disk_Mode`, mirroring `Version.Status.Working_Index_Mode`) instead of the index mode, so a chmod is visible in `diff`/`show`/`log -p` rather than silently masked.
- Fix: `Version.Grep.Match` gains a `Binary` flag; a binary file's matches are marked so the CLI prints `Binary file <path> matches` once (default output) while `-c`/`-l` still use the per-line matches, as git does.
- Fix: `Version.Init.Init`/`Init_Bare` are idempotent -- they reinitialise an existing repository (ensuring the directory tree, preserving an existing HEAD/config) instead of raising, matching git's re-init behaviour.
- Fix: `Version.Describe` computes tag distance with its own all-parents `<tag>..<commit>` walk instead of `Version.Rebase.Commits_To_Replay`, so `describe` no longer raises `rebase of merge commits not supported` on merge histories.
- Fix: `Version.Init` omits `core.logallrefupdates` from a bare repository's config (git parity).
- Fix: `Version.Notes.Add` writes git's `Notes added by 'git notes add'` commit message so the notes-ref object id matches git.
- Fix: `Version.Config` single-value `Unset_*` raises the new `Key_Absent` exception for a missing key (the CLI maps it to git's silent exit 5) instead of a generic `Data_Error`.
- Fix: `Version.Checkout.Checkout_Commit` gained a `Branch` argument -- when set it attaches HEAD to the named local branch symbolically (`ref: refs/heads/<name>`) instead of detaching at the raw commit, and writes git's `checkout: moving from <old> to <new>` HEAD reflog. This is what makes `checkout <branch>` stop orphaning subsequent commits.
- Fix: `Version.Config` now serializes values with git's `quote_value` rules (double-quote a value with leading/trailing space or a `#`/`;`; escape `\`, `"`, tab, newline) and its reader preserves quote-protected whitespace (git's pending-space rule), so a set/read round-trip is byte-faithful. A single-value `Unset_*` on a multivar now raises `Version.Config.Ambiguous_Key` and leaves the file untouched instead of dropping every value.
- Fix: `archive` now streams the archive to standard output when no `--output`/`-o` is given (git parity), instead of writing an `archive.tar` file into the working directory. The tar itself is now byte-identical to git: entries in recursive tree order (not all directories first), git's `tar.umask` modes (0664/0775, symlinks 0777), the commit's committer time as mtime, `root`/`root` owner, devmajor/devminor and checksum fields formatted as git does, and the archive padded to git's 20-block (10240-byte) record. Multi-level `--prefix` matches too (the prefix is one directory entry, not decomposed).
- Fix: `clean -fd` no longer deletes a nested git repository (a directory holding a `.git`) -- git preserves it and requires a doubled force (`-ff`), which version now honours (the `-f` count is threaded into `Version.Clean`). Previously `clean -fd` removed an embedded repo outright, silent data loss.
- Fix: `apply --index` now enforces git's precondition that each patched file already matches the index -- on a dirty working tree it refuses (`<path>: does not match index`, exit 1) and changes nothing, instead of silently applying to the working tree and staging the result. `--cached` (index only) stays exempt, and new-file creation is unaffected.
- Fix: `log` now walks the full commit history in commit-date order across ALL parents, not just the first-parent chain -- a merge previously hid every commit reachable only through its second-or-later parent from `log`/`--oneline`/`--stat`/`-p`/`--format`. Also adds the `Merge: <p1> <p2>` header line for merge commits and suppresses the per-merge diff under `--stat`/`-p` by default, both matching git.
- Git parity: new `Version.Dumb_Http` -- the dumb HTTP protocol's object walker: everything reachable from a commit fetched over ordinary GETs, loose objects written straight to disk (they arrive in loose form already) and, when the server keeps an object packed, the packs from `objects/info/packs` downloaded and indexed. `Version.Transport.Http.Get` is the plain GET it rides on; it reports a 404 as "not here" instead of raising, which is how the walker learns to go look in the packs.
- Git parity: new `Version.Multi_Pack_Index` -- writes git's `multi-pack-index` byte-for-byte (MIDX header, PNAM/OIDF/OIDL/OOFF chunks with git's 4-byte PNAM padding, and the trailing checksum), resolving an object held by several packs the way git does (the newer pack wins) and dropping a pack that ends up contributing nothing. 38/40 randomized repositories byte-identical, all git-verified. BOUNDARY: when two packs were written within the same *second*, git compares their sub-second mtimes while Ada's `Modification_Time` only resolves seconds, so version falls back to the lower-numbered pack and may attribute a duplicated object to the other pack. The file stays valid (git verifies it); real repositories, whose packs are written seconds apart, are byte-identical.
- Git parity: new `Version.Commit_Graph` -- writes git's `commit-graph` file byte-for-byte (CGPH header, OIDF/OIDL/CDAT/GDA2 chunks, plus EDGE when an octopus merge needs it, and the trailing checksum), with git's generation numbers: the topological level in CDAT and the corrected-commit-date offset in GDA2. 50/50 randomized histories byte-identical and accepted by `git commit-graph verify`, octopus merges included.
- Git parity: `Version.Staging` honours `GIT_INDEX_FILE` (it always used `<git-dir>/index`). git points the index elsewhere for `filter-branch --index-filter` and for reading a tree into a temporary index; version ignored it.
- `refs/original/` is now an accepted ref namespace (filter-branch keeps the pre-rewrite tip there).
- `Version.Diff.Unified_Blob_Diff` exposes one file's patch (the `diff --git`/`index` headers and hunks) for callers that already have the two blobs, which is what `diff-pairs` renders.
- Git parity: new `Version.Fetch.Fetch_Objects_From` and `Version.Push.Push_Refspec_To` -- fetch/push through a path or URL that is not a configured remote (the scratch-remote dance, cleaned up afterwards), which is what `fetch-pack`/`send-pack` take.
- Git parity: new `Version.Mailbox` (`Is_From_Line`/`Split`/`Parse`) -- mbox splitting and mail parsing, extracted from `Version.Am` so `mailsplit`, `mailinfo` and `am` share one parser. `am`'s From-line rule is now git's (mailsplit.c: any mbox From line with a time in it), not "From " + 40 hex.
- `Version.Pack.Index_Pack` takes `Canonicalize` (default True, the fetch path's behavior): with it off the pack stays where it is and only the index is written, which is what `index-pack <file>` means.
- `Version.Branch.Shift_Subtree_Items` exposes the subtree strategy's tree shift (prefix inferred from the two trees) so the `merge-subtree` plumbing can use it.
- Git parity: new `Version.Attributes` -- a real `.gitattributes` engine (previously each consumer parsed the file for its own attribute). It is a port of git's resolution: highest-precedence file first, only ever filling an attribute that is still unknown, tokens within a line walked in reverse, and a macro expanded *only* when it resolves to "set" (so `!binary` blocks the macro instead of expanding it). Patterns match literally -- gitignore's "a directory carries its contents" rule does not apply, and a trailing-slash pattern matches only a path spelled with one.
- Git parity: new `Version.Mailmap` (`Load`/`Parse`/`Apply`), extracted from `Version.Pretty_Format` so `check-mailmap` and `%aN` share one implementation.
- Git parity: new `Version.Fetch.List_Remote_Refs` -- every ref a remote advertises (local, HTTP, or SSH), HEAD first, annotated tags followed by their peeled `^{}` entry.
- `Version.Ignore.Wildcard_Matches` exposes git's pathname-semantics wildmatch for reuse.
- Git parity: new `Version.Subtree` (`add`/`merge`/`pull`/`split`/`push`) -- a port of `git subtree`, including its split algorithm: prior `git-subtree-dir:`/`-mainline:`/`-split:` joins seed the commit map, a parent already carrying the identical subtree is reused rather than copied, and copies preserve the original author *and* committer verbatim. Split lineages are byte-identical to git's.
- Git parity: commit, tag, and merge identities now honour `GIT_AUTHOR_NAME`/`_EMAIL`/`_DATE` and `GIT_COMMITTER_*` (`Version.Config.Author_Signature`/`Committer_Signature`), and record the local timezone offset instead of a hardcoded `+0000` -- the same commit content now hashes to the same object id as git's.
- Git parity: a merge with no `-m` names its target the way git does -- `Merge branch 'x'`, `Merge tag 'v1'`, `Merge remote-tracking branch 'origin/x'`, or `Merge commit '<rev-as-given>'` (was: `Merge <abbrev>`).
- Fix: `Version.Remotes.Delete_Remote` now deletes the remote-tracking refs along with the configuration, as `git remote remove` does; `refs/remotes/<name>/*` used to survive the removal.
- Fix: `Version.Blame` now runs the line-correspondence diff with git's indent heuristic, which `git blame` uses. Blame is byte-identical to `git blame` across randomized edit histories (450/450, including indented source); the residual duplicate-line tie-break is gone.
- Added `Version.Pack.Read_Delta_Base`: where a delta-encoded entry's base lives (a pack offset for an OFS delta, an object id for a REF delta), which `verify-pack` needs to report chain depth and base ids.
- Fix: `Version.Checkout` refused to check out while *any* untracked file existed. git only refuses when the commit being checked out would overwrite one; the blanket rule made ordinary situations (a build artifact in the tree, `bisect run`'s own test script) impossible. It now rejects only the paths the checkout would actually overwrite.
- Fix: `Version.Status`'s new mode comparison excluded gitlinks -- a submodule is a directory, not a file, and comparing its "worktree mode" reported every submodule as permanently modified (which then blocked merges and branch switches as an unclean tree).
- Git parity: `Version.Diff` now uses git's own diff engine (the ported xdiff, via the new `Version.Merge.Text_Changes`) instead of a home-grown LCS -- including git's **indent heuristic** (`diff.indentHeuristic`, which `git diff` enables by default and the merge machinery does not: a new port of `measure_split`/`score_add_split`/`score_cmp`). Hunks now land exactly where git puts them; `version diff` used to disagree with `git diff` on ~12% of inputs. This is the last home-grown diff in the codebase.
- Added `Version.Log`'s `Patch`/`Context` options: git's `-p`/`--patch` and `-U<n>` after each commit.
- Fix: `Version.Stash`'s apply/pop no longer refuses when untracked files are present. They are not part of what a stash restores, git ignores them here, and refusing made `stash pop` fail whenever a single stray new file sat in the tree.
- Git parity: `Version.Status` detects renames. A staged delete and a staged add are reported as one `Renamed_File` (with the source in the new `File_Change.Old_Path`) when their contents match exactly, or are at least 50% similar -- git's default threshold. Pairing follows git's `diffcore-rename`: an exact-blob pass first, then a destination-major similarity pass (source-major greed lets a poorer source claim a destination a better one should win). Honours `status.renames`.
- Fix: `Version.Blame` built its longest-common-subsequence table in an *uninitialised* heap matrix (`new Matrix (0 .. A, 0 .. B)` -- Ada does not zero it, and the recurrence reads row/column 0). Blame was computing line correspondences from heap garbage and attributing lines to the wrong commits. The alignment now comes from git's own diff (new `Version.Merge.Align_Lines`, over the ported xdiff Myers), and the home-grown LCS is gone.
- Git parity: `Version.Status` now reports mode-only changes (a `chmod +x` with untouched content is a change to git, and blob ids alone never saw it), and collapses a directory holding nothing but untracked files to `dir/` as git does, with `All_Untracked` for git's `-uall`.
- Fix: `Version.Repository` accepted a `.git` *file* only when its `gitdir:` path contained no `..` -- but every submodule git creates points upwards (`gitdir: ../.git/modules/<name>`), so no git-created submodule could be opened at all. Relative gitdirs are now resolved as git does (the `commondir` reader already did).
- Git parity: a submodule (gitlink) is rendered in diffs and `--stat` as the one-line text `Subproject commit <sha>` instead of being read as an object of the outer repository (which failed with "object not found"). `version diff` of a submodule bump now matches git byte for byte.
- Git parity: merging a submodule reports `Note: Fast-forwarding submodule <path> to <sha>` and, like git, moves only the gitlink -- the submodule's working tree is left alone (`submodule.recurse` defaults to false; version defaulted to true and checked the submodule out).
- Git parity: an external `merge.<driver>.driver` exiting non-zero means "conflicts remain", not "I did nothing": its output in `%A` is kept as the merge result and the path is recorded as conflicted. version used to discard the driver's work and write its own conflict markers instead.
- Git parity: `merge.renormalize` now re-runs the text clean filter (CRLF -> LF) on all three blobs before merging, so a side that merely re-committed a file with different line endings is no longer seen as having changed it.
- Git parity: `Version.Merge.Merge_File` now implements git's two merge *levels*. `git merge-file` runs xdiff at XDL_MERGE_ZEALOUS_ALNUM, which also folds the lines between two conflicts into a single conflict when none of them contains a letter or digit (blank lines, braces, punctuation) -- however many there are; `git merge` runs at XDL_MERGE_ZEALOUS and only ever folds gaps of at most three lines. `Merge_File_Options.Simplify_No_Alnum` selects the level (default: merge-file's).
- Git parity: `Diff_Algorithm_Patience` is now a port of git's xdiff `xdl_do_patience_diff` (anchor on the lines occurring exactly once on each side, take the longest increasing subsequence of those anchors, grow each into a run of equal lines, recurse between them, and fall back to Myers where no unique common line exists) instead of version's own LCS. With that, `Version.Merge` no longer contains any home-grown diff: every algorithm -- myers, minimal, patience, histogram -- is git's.
- Fix: a rename/modify conflict now writes stage 1/2/3 index entries at the rename's destination, as git does (it previously recorded the conflict and wrote the markers but left the index without unmerged stages, so `version add`/`git ls-files -u` could not see it).
- Git parity: `Version.Merge` now diffs merges the way `git merge` does. git's merge machinery (merge-ort) runs the diff with **histogram**, not Myers -- only `git merge-file` defaults to Myers -- so `Version.Merge.Merge_File` gained git's histogram engine (a port of xdiff's `xdl_do_histogram_diff`: index each line's occurrences on one side, anchor on the *least* repeated common line, recurse either side, fall back to Myers when a region is too repetitive), plus the histogram-only re-diff that `xdl_change_compact` performs after it merges two change groups. `Merge_Behavior`'s default algorithm now resolves to histogram for tree merges, while `merge-file` keeps Myers; `-Xdiff-algorithm=` still overrides.
- Git parity: the ignore-whitespace merge modes are now honoured by the diff itself, not just by a whole-file equivalence check. Lines are folded per git's `xdl_recmatch`/`xdl_hash_record_with_whitespace` (`Normalize_Line`) for every comparison -- classification, change compaction, conflict refinement, the identical-edit check and the zdiff3 edge trim -- while emission always uses the original, unfolded lines. A side whose only change is ignorable whitespace now counts as having made no change, so the merge resolves to the other side's file verbatim, as git's `xdl_do_merge` does. The "both sides did the same thing" shortcut now compares exact bytes (git compares object ids), so it can no longer preempt that rule.
- Git parity: conflict markers name each side's own path when a rename means the two sides carried the content under different names (`<<<<<<< HEAD:old.txt` / `>>>>>>> feature:new.txt`); sides that renamed to the same path keep the plain labels.
- Added `Version.Diff.Unified_Text_Diff`: a unified diff of two texts with only the `--- a/<path>`/`+++ b/<path>` header and the hunks, which is the shape `git rerere diff` prints.
- Git parity: `Version.Merge`'s diff engine is now a port of git's own (xdiff `xdl_do_diff`): lines are classified into equivalence classes, the common head/tail is trimmed, records that cannot match are discarded (`xdl_cleanup_records`, including the multi-match run heuristic), and the edit script comes from Myers' O(ND) divide-and-conquer (`xdl_recs_cmp`/`xdl_split`) with git's snake and cost heuristics -- replacing version's own LCS for the default/myers/minimal algorithms (`--diff-algorithm=minimal` maps to xdiff's need-minimal flag; patience/histogram keep the existing engine). An equally minimal script is not enough for parity: which script you pick decides where a hunk boundary falls, and therefore where a merge conflict lands. `merge-file` is now byte-identical to `git merge-file` on 100% of fuzzed 3-way merges (previously ~88%) across the default/diff3/zdiff3/ours/theirs/union modes, LF and CRLF, from tiny inputs up to 2500-line files that exercise git's cost cutoff.
- Git parity: conflict markers now follow the file's line-ending style -- a CRLF file gets CRLF-terminated `<<<<<<<`/`|||||||`/`=======`/`>>>>>>>` markers (and CRLF for any newline the merge has to add), via a port of git's `is_cr_needed`/`is_eol_crlf` from xdiff/xmerge. LF-only files are unaffected. Byte-identical to git across the merge/diff3/zdiff3 styles, mixed line endings, and files with no trailing newline.
- Git parity: `Version.Merge`'s tree merge now resolves text conflicts hunk by hunk through `Merge_File` (git's `ll_merge`) instead of wrapping the whole file in one conflict block. Adds git's `xdl_change_compact` (slide each change group to where the other side's group starts, so both sides agree on where a change begins) and `xdl_refine_conflicts` (re-diff a conflict's two sides and split it at the lines that really differ), so cleanly-merged regions stay outside the markers, add/add merges against an empty base, `-Xours`/`-Xtheirs` and the `union` attribute resolve individual hunks rather than discarding the other side's clean ones, and conflict labels are git's (`HEAD`, the abbreviated merge-base oid for the diff3 base, `Updated upstream`/`Stashed changes` for stash, `<abbrev> (<subject>)` for the replayed commit). Conflicted files, index stages, and rerere preimages/rr-cache ids are now byte-identical to git. Note: version previously auto-merged edits on *adjacent* lines; git conflicts on those (it needs at least one common line between the two sides' changes) and version now does too.
- Reworked `Version.Merge`'s rerere store to git's on-disk format: added `Rerere_Conflict_Id` (per-file conflict hash = SHA-1 of each conflict's `min_side\0max_side\0` with sorted sides -- byte-identical to git's rr-cache dir name), normalized preimages (bare markers, context outside, diff3 base dropped), and NUL-terminated MERGE_RR. Record and replay now key off the conflict content instead of the merge's commit trio, so a resolution is reused for the same textual conflict from either side. (rr-cache hashes equal git's only for git-granularity conflicts; version's merges still emit coarser conflict blocks -- see merge-file note.)
- Added `Version.Merge.Merge_File`, a `git merge-file`-compatible hunk-level 3-way merge: a faithful port of xdiff/xmerge's `xdl_do_merge` over version's `Diff_Edits` change scripts -- the overlap walk + `xdl_append_merge` region-extend, the one-sided tails, the zealous conflict-combine pass (adjacent conflicts <=3 common lines apart, default style only), and the marker fill-buffer with the newline-before-marker rule. Supports merge/diff3/zdiff3 styles (incl. the zdiff3 edge refinement), ours/theirs/union favor, custom labels, and marker size; returns the conflict count. Byte-identical to git (version now reproduces git's own edit script -- see the diff-engine entry above).
- Added `Version.Show_Branch.Format`, a `git show-branch`-compatible renderer: resolves the named branch tips, computes the commit set from the tips down to their merge base, orders it newest-committer-date-first, propagates first-parent names in that display order (newest tip names shared commits first, so naming is argument-order-independent), and emits the header/separator/marker matrix (`*` current, `+` other, `-` merge, space unreached) plus the `--list` head summary. Non-merge ranges are byte-identical to git.
- Added `Version.Bisect`, a `git bisect`-compatible state machine and commit-selection engine: session state under the git dir (`refs/bisect/<term>`, `BISECT_START`/`TERMS`/`LOG`), and `Compute` reproducing git's bisection -- candidate set = commits reachable from bad but no good, weight = ancestor count within that set, oldest-first strict-max-distance selection (with git's 3-candidate tie exception), `Left = all - reach - 1`, and a step estimate matching `git rev-list --bisect-vars` for sets up to 129.
- Git parity: `Version.Objects.Read_Object` now honors `refs/replace/<oid>` redirects (following chains, disabled by `GIT_NO_REPLACE_OBJECTS`), read directly from the ref file to avoid a Version.Refs dependency cycle; `Version.Ref_Names.Is_Valid_Ref_Name` now accepts the `refs/replace/` namespace. This makes `replace` genuinely functional, not just ref bookkeeping.
- Added `Version.Diff.Raw_Diff_Trees`, `Raw_Diff_Index`, and `Raw_Diff_Files`, producing git's raw diff format (`:<mode1> <mode2> <sha1> <sha2> <status>\t<path>`) for tree-vs-tree (recursive or single-level), tree-vs-index/working, and index-vs-working comparisons -- the engine behind `diff-tree`/`diff-index`/`diff-files`, byte-identical to git (including the zeroed working-side id and the staged/unstaged distinction).
- Git parity: `Version.Archive` now prepends a POSIX pax global header (`comment=<commit-id>`) when archiving a commit-ish to tar, matching `git archive`; added `Version.Tar.Add_Pax_Global_Header`. This lets `get-tar-commit-id` recover the id from a version-produced tar.
- Added `Version.Fmt_Merge_Msg.Format`, a `git fmt-merge-msg`-compatible merge-message builder: parses FETCH_HEAD lines (skipping not-for-merge), groups merge heads by origin/type ("branch 'a' and 'b' of <url>"; local refs stand alone joined by "; "), appends " into <branch>" off the default, and inlines merged annotated-tag messages.
- Added `Version.Write.Write_Object`, which stores a loose object of any kind ("blob"/"tree"/"commit"/"tag") from its exact payload bytes and returns its id (used by `mktag`).
- Added `Version.Write.Write_Tree`, which writes a single-level tree object from explicit entries (as `git mktree`): git tree-order sorting (directories compared with a trailing `/`), leading-zero mode canonicalisation, and loose-object serialisation -- oid-identical to `git mktree`.
- Added `Version.Ref_Names.Is_Valid_Check_Ref_Format` and `Normalize_Ref_Format`, a `git check-ref-format`-compatible general refname-grammar check (any category, `--allow-onelevel`, `--refspec-pattern` single-`*` glob) and slash normaliser -- distinct from the stricter storage-policy `Is_Valid_Ref_Name`.
- Added `Version.Stripspace.Clean`, a `git stripspace`-compatible message cleaner: trailing-whitespace stripping, blank-run collapsing, leading/trailing blank trimming, `--strip-comments` (drop `#` lines), and `--comment-lines` (prefix every line with `# `).
- Added `Version.Trailers.Interpret`, a `git interpret-trailers`-compatible message-trailer engine: trailer-block detection (last non-first paragraph containing a `token: value` line), add-after/add-before placement, `--only-trailers`/`--only-input`/`--unfold` extraction, and `:`/`=` separator normalisation.
- Git parity: `Version.Revisions.Resolve` handles `<ref>@{n}` reflog syntax (@{0} = current value; bare `@{n}` = HEAD), and `Version.Ref_Format` gained the `%(upstream)`/`%(upstream:short)` atoms (a branch's configured remote-tracking ref).
- Added `Version.Pack.All_Pack_Objects`, which enumerates every object id in the repository's `*.idx` (v2) pack index files (fanout[255] count + the sorted name table).
- Git parity: `Version.Log.Log_From_Commit`/`Log_Head` gained a `Stat` option that appends git's `--stat` diffstat (against the first parent, or the empty tree for a root commit) after each commit.
- Bug fix: `Version.Diff`'s LCS matrix was allocated with `new` (uninitialised) and its base-case boundary row/column were never zeroed, so the dynamic-programming table read garbage and produced non-minimal diffs (unchanged lines deleted and re-added instead of kept as context). Zeroing the boundary makes `diff`/`show`/`log --stat`/`format-patch` produce git's minimal, byte-identical hunks.
- Git parity: `Version.Diff` gained `--name-only`/`--name-status` (changed paths, optionally prefixed with git's A/D/M status), and `Version.Log.Log_Formatted_From_Commit` renders a first-parent walk through `Version.Pretty_Format` (git's `--format`/`--pretty=format:`/`tformat:`, with the right per-record vs separator newline).
- Git parity: `Version.Pretty_Format` `%b` and `%B` now end with git's single trailing newline (`%b` is empty with no newline when there is no body), instead of dropping it. `Version.Notes.Add` now normalises a note message the way git does — trailing blank lines/whitespace stripped and exactly one trailing newline ensured — so a version-written note object is byte-identical to git's (same SHA), and `version notes show` emits the note verbatim (via `Version.Console.Put`) rather than doubling its final newline.
- Bug fix: `Version.Upload_Pack.Build_Want_Request` (filtered fetch/partial clone) now advertises the `filter` capability in the want line. Without it, upload-pack rejected the following `filter <spec>` command and returned no usable pack, so `clone --filter=...`/`fetch --filter=...` over a filter-enabled HTTP/SSH server failed with "upload-pack response did not provide requested commit". Filtered clones now complete and are `git fsck`-clean.
- Bug fix: object reachability (`Version.Reachability.Repository_Roots`, used by `verify`/`gc`) is now reftable-aware — it enumerates branch/tag/remote roots from the reftable stack rather than walking `.git/refs/heads` etc. A git-created reftable repository leaves those as stub files, which previously crashed the loose-ref walk with `unknown directory "heads"`. The loose/reflog directory walkers also now skip a non-directory path defensively.
- Git parity: `Version.Push.Delete_Ref` (`push --delete`, `push :REF`) now resolves an unqualified ref name against the remote's advertised refs — preferring a matching branch, then a tag — instead of always assuming `refs/heads/`. Deleting a short tag name (e.g. `push --delete origin v1`) now removes `refs/tags/v1` like git, across local, HTTP, and SSH remotes.
- Git parity: `Version.Describe.Describe` now considers only annotated tags by default (git's rule) and gains an `All_Tags` argument (the CLI's `describe --tags`) that also considers lightweight tags. Its failure messages now mirror git: "No names found, cannot describe anything." when no tag is reachable, and "No annotated tags can describe '<oid>'." with the "However, there were unannotated tags: try --tags." hint when only lightweight tags are reachable. Previously a lightweight tag was silently used by default.
- Git parity: `Version.Push.Push_Default` (a `push`/`version push REMOTE` with no refspec) now falls back to `push.default` when no `remote.<name>.push` is configured, instead of erroring. It implements git's `simple` (default), `current`, `upstream`/`tracking`, and `nothing` modes — pushing the current branch to its same-named or upstream branch — and reproduces git's errors for a missing upstream, a `simple` name mismatch, `nothing`, and a detached HEAD. (`matching` continues to route to `Push_Matching`.)
- Git parity: `Version.Write.Save`/`Save_Amend` now honour commit signing — they read `commit.gpgSign` (with the key from `user.signingKey`, else gpg's default) and gain a `Sign`/`Signing_Key` parameter so the CLI's `-S`/`--gpg-sign[=KEY]`/`--no-gpg-sign` can force or suppress signing. Previously a configured `commit.gpgSign` (or `save -S`) silently produced an unsigned commit. Config reads are guarded with `Has_Key` so an unconfigured `user.signingKey`/`commit.gpgSign` no longer raises "config key does not exist" from every unsigned commit.
- Bug fix: `Version.Rebase.Start_Interactive` now aborts an emptied/all-comment todo with "nothing to do" and leaves the branch untouched (git's safety net), instead of resetting the branch to the upstream and silently discarding every commit. An explicit `drop` of every commit is still a successful rebase that advances to the upstream — the two are distinguished by whether any command line was seen.
- Git parity: `Version.Clean.Candidates` with `-d` now also removes untracked directories that hold no untracked file (an empty directory, or a nested tree of empty directories collapsed to its shallowest member) by walking the worktree — these contribute no entry to the untracked-file scan. An ignored empty directory still needs `-x`, and symlinks are treated as files (never descended), matching git.
- Windows parity: `Version.Files.To_Native_Path` now applies git-for-Windows's `\\?\` extended-length prefix (and `\\?\UNC\` for UNC paths) to a fully-qualified Windows path of at least MAX_PATH (260) characters, so long absolute paths remain usable via the Win32 API. Relative and short paths are unchanged, and the transformation is a no-op on POSIX. (Runtime behavior on a real Windows host is not part of the Linux test matrix; the path transformation is unit-tested by forcing the platform to Windows.)
- Git parity: `Version.Submodules` gains `Foreach`, `Sync`, and `Deinit` (git's `submodule foreach`/`sync`/`deinit`), operating over the existing `.gitmodules`/gitlink machinery and writing `submodule.<name>.*` config via `Version.Config`. `Version.Working_Tree`'s untracked scan now excludes a `.git` gitfile (not only a `.git` directory), so a submodule/linked-worktree `.git` pointer is no longer reported as untracked.
- Git parity: shallow deepening. `Version.Fetch.Fetch_Deepen` (`fetch --deepen N`) and `Version.Fetch.Fetch_Unshallow` (`fetch --unshallow`) extend or flatten a shallow repository; `Version.Upload_Pack.Build_Want_Request` gained `Relative` (emits the `deepen-relative` capability) and `Have_Shallow` (echoes the client's current boundary as `shallow <oid>` lines), and unshallow uses `deepen 2147483647`. The existing `shallow`/`unshallow` response handling then updates (and removes) `.git/shallow`.
- Bug fix: `Version.Pack.Index_Pack` now finalizes a fetched pack to git's canonical `pack-<checksum>.{pack,idx}` name rather than leaving it at the caller's temporary path, so repeated fetches/clones into one repository no longer collide on a fixed filename and destroy earlier objects.
- Git parity: cone-mode sparse-checkout is now writable. `Version.Sparse.Set_Cone` writes git's cone patterns and sets `core.sparseCheckoutCone`; `Cone_Recursive_Directories` reports the leaf directories for `list`; `Disable` keeps the pattern file (only clearing the config flags); and the cone read logic (`Included`) now honours the `!/dir/*/` ancestor exclusions so a parent directory's own files are kept while its sibling subtrees are excluded. `Version.Staging.Index_Entry` gained a `Skip_Worktree` flag that `Load`/`Write` round-trip (writing a version-3 index with the extended-flags word when any entry sets it), and `Version.Restore.Apply_Sparse_Skip_Worktree`/`Clear_Skip_Worktree` set or clear those bits from the sparse pattern set. `Version.Files.Is_Ordinary_File` returns `False` for an empty path instead of raising.
- Git parity: `Version.Clone.Clone` now tolerates a dangling remote HEAD (the remote's HEAD names a branch that was not fetched): instead of raising, it points HEAD at the unborn default branch, records its `[branch]` upstream config, leaves the worktree unchecked-out and writes no `origin/HEAD`, and warns `remote HEAD refers to nonexistent ref, unable to checkout` — matching `git clone`.
- Git parity: `Version.Config.Read_All` now reads git's full config scope stack — system (`/etc/gitconfig` or `GIT_CONFIG_SYSTEM`, honouring `GIT_CONFIG_NOSYSTEM`), global (`$XDG_CONFIG_HOME/git/config` then `~/.gitconfig`, or `GIT_CONFIG_GLOBAL` replacing both), then the local and per-worktree config — rather than the local config alone, so `Get_Value`/list/keys resolve values defined in the developer's global or system config. `Version.Files.Is_Ordinary_File` now treats an empty path as "not a file" (returns `False` rather than raising `Name_Error`) so suppressed scopes probe cleanly. (`GIT_CONFIG_COUNT` env injection is not yet read.)
- Git parity: `Version.Config.Read_All` now follows `[include]`/`[includeIf]` directives (unconditional plus `gitdir:`, `gitdir/i:`, `onbranch:`, and `hasconfig:remote.*.url:` conditions), resolving include paths relative to the including file with `~` expansion and bounding recursion depth; the directive stays a readable entry as git does. `Get_Value` now returns the **last** matching entry (git's single-value semantics) and `config.worktree` is layered after the common config to match; `Config_Entry_Name` lower-cases the section and variable names (subsection preserved) like git.
- Git parity: `Version.Pathspec` now supports the `:(icase)` magic — a matching item folds ASCII case for the pattern and path while keeping the real path for attribute checks.
- Git parity: SHA-256 object-format repositories are now fully supported, including over the smart HTTP/SSH transport. `extensions.objectFormat = sha256` repositories open (the compatibility gate is lifted), `Version.Init.Init`/`Init_Bare` take an object-format argument, and every object/index/pack/ref/reflog code path is hash-width-aware. The smart transport negotiates the object format (`Version.Upload_Pack.Advertised_Object_Format` reads the advertised `object-format` capability, `Version.Fetch.Remote_Object_Format` discovers it before a clone creates its target, and the receive-pack client sends `object-format=sha256` and repo-width null ids for ref create/delete). Verified against system git — `git fsck`, `verify-pack`, `bundle verify`, `reflog`, and clone/push/fetch over both local and real `git http-backend` SHA-256 servers accept version's output, and version reads git-created SHA-256 repositories (Phases 4–5 of SHA-256 support).
- Added a SHA-256 implementation to `Version.Hash` (`Sha256_Hex`/`Sha256_Raw` plus a streaming `Sha256_Context`) and a `Hash_Algorithm` descriptor (`Hex_Length`/`Raw_Length` = 40/20 for SHA-1, 64/32 for SHA-256) — Phase 0 of SHA-256 object-format support (see `../version/docs/SHA256_SCOPE.md`); no object-format behavior change yet.
- Added `Version.Clone.Clone_Filtered` and `Version.Fetch.Fetch (Remote, Filter_Spec)` for partial clone: HTTP/SSH send the filter when the server advertises the `filter` capability; local transport selectively copies non-blob (or size-bounded) objects via the new `Version.Write.Copy_Object`. The clone is configured as a partial clone, and the local promisor lazy fetch (`Fetch_Object`) now copies only the requested object instead of the whole store.
- Supported the `extensions.worktreeConfig` repository extension: `Version.Config.Read_All` now layers the per-worktree `config.worktree` over the common config (per-worktree values override; absent keys fall back), and `Version.Repository_Format` no longer rejects it.
- Added `Version.Push.Push_Default` (apply the configured `remote.<name>.push` refspec(s) — multi-valued, parsed like command-line refspecs incl. `+`/`:` — via `Version.Config.Read_All`).
- Added a `Force` parameter to `Version.Push.Push_Tags` and `Version.Receive_Pack.Push_Tag`/`Push_Tag_Ssh` (and the local tag-write path) that overwrites a differing remote tag instead of refusing.
- Added `Version.Push.Push_Refspec` and `Version.Receive_Pack.Push_Ref`/`Push_Ref_Ssh` (push an arbitrary source commit to a named remote ref over local, HTTP, or SSH, with a fast-forward check unless forced) — the engine behind `push SRC:DST`.
- Added `Version.Push.Delete_Ref` and `Version.Receive_Pack.Delete_Ref`/`Delete_Ref_Ssh` (delete a remote ref over local, HTTP, or SSH via a delete-only receive-pack command); relaxed `Build_Update_Command` to accept `refs/tags/` (fixing tag push over HTTP/SSH).
- Added a `Force` parameter to `Version.Push.Push` and `Version.Receive_Pack.Push_Branch`/`Push_Branch_Ssh` (and the local branch path) that skips the fast-forward (ancestor) check for non-fast-forward updates.
- Added `Version.Receive_Pack.Push_Tag` / `Push_Tag_Ssh` (push `refs/tags/*` over smart HTTP / SSH receive-pack, refusing to overwrite a differing remote tag); `Version.Push.Push_Tags` now uses them for HTTP/SSH remotes instead of rejecting them, and removed `Version.Unsupported.Push_Tags_Non_Local_Remote`.
- Added `Version.Objects.Tree_Entries` (single-level, non-recursive tree parse returning subtrees as `Tree_Directory` entries) and `Version.Refs.Write_Symbolic_HEAD` (atomically point HEAD at a symbolic ref).
- Replaced the system-`ssh` subprocess SSH transport with native `ssh_lib`: `Version.Transport.Ssh` now opens an `SSH_Lib.Sessions` session and runs `git-upload-pack`/`git-receive-pack`/`git-lfs-authenticate` over `SSH_Lib.Channels.Open_Exec` (streaming via `Read_Some`/`Write`), with no dependency on an external `ssh` binary. Connection settings are resolved through the user's `~/.ssh/config` via `SSH_Lib.Config.Resolve_Remote` (HostName aliases, Port, User, IdentityFile, known_hosts), with the URL's explicit user/port taking precedence. The pure URL/command-builder API is unchanged.
- Added `Version.Rebase.Start_Rebase_Merges` (topological replay recreating two-parent merges via `Merge_Trees` + `Write_Commit_With_Author`).
- Added `Version.Rebase.Start_Root` (rebase the whole branch onto a new base) and root-commit replay in `Replay_Commit` (empty base tree, gated by `Allow_Root`).
- Added `Version.Rebase.Start_Interactive` (sequence-editor todo): pick/drop/reorder replay through the existing state machine; squash/fixup combine commits via a one-shot executor (using `Write_Commit_With_Author`).
- Added `Version.Shortlog`, `Version.Grep`, `Version.Describe`, `Version.Notes`, and `Version.Blame`; allowed the `refs/notes/` namespace in ref-name validation (for notes).
- Added `Version.Patch_Id` (normalized-diff change identity), `Version.Cherry`, and `Version.Range_Diff`.
- Added `Version.Am` (split an mbox, reverse-parse RFC2822 dates, apply each patch, stage, and commit with the patch's author) plus `Version.Write.Write_Commit_With_Author`.
- Added `Version.Format_Patch` (render a commit as an mbox record: From/Date (RFC2822)/Subject headers, body, and the diff against its first parent).
- Added `Version.Apply` (parse and apply a unified diff: modify/create/delete, `-p1`, final-newline handling, context verification, validate-before-write).
- Added `Version.Bundle` (write a complete v2 bundle via Reachable_Objects + Write_Pack, and parse a bundle's ref/prerequisite header).
- Added `Version.Clean` (enumerate untracked/ignored removal candidates, collapsing fully-untracked directories like git, and delete them).
- Added `Version.Move` (rename a tracked file: move the working-tree file and restage source to destination).
- Added `Version.Reflog.Read_Entries`, parsing a ref's reflog file into oldest-first entries (old id, new id, message) for the `reflog` command.
- Added `Version.Reset` (reset to a commit in soft/mixed/hard modes, and path-form index reset), composing the existing revision/ref/index/restore engines.
- Established versionlib `0.1.0-dev` as a standalone library crate holding all version-control functionality (every Version.* package), extracted from the version crate, which now contains only the CLI; pre-split functionality history is in ../version/CHANGELOG.md.
